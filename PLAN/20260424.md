# Full Stack Monorepo Plan

## 目标
- 在 `D:\ce\full-stack-monorepo` 初始化一个可扩展的 full-stack monorepo。
- 先将现有 NestJS 后端完整复制到 `backend/`。
- 后续预留 `apps/` 与 `packages/` 给前端项目和共享模块。

## 执行清单
- [x] 确认 monorepo 首版目录结构
- [x] 创建根目录基础文件
- [x] 创建 `backend/`、`apps/`、`packages/`、`docs/`
- [x] 复制 `D:\ce\nestjs-agent-api` 到 `D:\ce\full-stack-monorepo\backend`
- [x] 检查后端 `package.json` 与 Nest 配置
- [x] 增加根目录 workspace 配置
- [x] 增加根目录统一启动脚本
- [x] 补充根目录 README

## 建议目录
```txt
D:\ce\full-stack-monorepo
  backend/
  apps/
  packages/
  docs/
  package.json
  pnpm-workspace.yaml
  .gitignore
  README.md
  PLAN/
```

## 说明
- 当前阶段优先保证“先复制能跑”。
- `backend/` 先保持原后端结构，避免一开始就重构成 `apps/api-nest`。
- 前端项目当前仍放在 `apps/` 下管理。

## 前端应用
- [x] 创建 `apps/liberty-web` Next.js 空项目
- [x] 创建 `apps/liberty-ui` UmiJS 空项目
- [x] 接入根目录启动与构建脚本

## 根目录 Alias
- [x] 创建根目录 `web/`
- [x] 增加根级 `tsconfig.base.json`
- [x] 配置根级 `@packages/*`、`@web/*`、`@backend/*`
- [x] 同步 `backend` 的 TypeScript 路径配置
- [x] 取消 `liberty-web` 对根 `tsconfig.base.json` 的继承
- [x] 取消 `liberty-ui` 对根 `tsconfig.base.json` 的继承
- [ ] 验证 VSCode 可跳转与补全
