# 待确认决策清单

- 生成日期：2026-04-27
- 适用项目：`apps/liberty-ui`
- 对应后端：`backend/agent-nestjs`
- 关联实施计划：`apps/liberty-ui/PLAN/1.后台系统建设计划.md`
- 文档目标：明确后台系统在正式开发前、联调中、回归前必须拍板的前端产品 / 交互 / 权限 / 展示决策，避免页面实现偏差与返工。
- 使用方式：
  - 每个问题都需要产品、业务、前端负责人或联调负责人给出明确结论。
  - 一旦确认，直接在对应条目下补充“最终决策”与“决策日期”。
  - 已确认项改为 `[x]`，未确认项保持 `[ ]`。

## 1. 登录与登录态决策

### 1.1 登录页展示范围
- [x] 登录页只保留 `account + password`
- [x] 登录页不展示注册入口
- [x] 冻结 / 禁用提示直接显示后端返回 `msg`
- [x] 登录页前端校验密码格式：大小写字母 + 数字，至少 8 位
- [x] 登录页前端禁止空提交

最终决策：
- 登录页只保留 `account + password`
- 登录页不展示注册入口
- 业务失败时前端直接显示后端返回 `msg`
- 密码错误提示为：`密码错误`
- 账号禁用提示为：`该账号已被禁用`
- 登录页前端校验密码格式：大小写字母 + 数字，至少 8 位
- 登录页前端禁止空提交

决策日期：2026-04-27

### 1.2 登录态持久化策略
- [x] `accessToken` 存本地
- [x] `refreshToken` 存本地
- [x] `currentUser` 存本地
- [x] refresh 失败后统一跳转 `/login`

最终决策：
- `accessToken`、`refreshToken`、`currentUser` 全部存本地
- refresh 失败后统一清空登录态并跳转 `/login`

决策日期：2026-04-27

### 1.3 refresh 交互策略
- [x] access token 过期时自动 refresh
- [x] refresh 成功后重放原请求
- [x] refresh 需要并发锁，避免重复刷新

最终决策：
- access token 过期时自动 refresh
- refresh 成功后自动重放原请求
- 需要 refresh 并发锁，避免多个请求同时刷新

决策日期：2026-04-27

## 2. 布局与路由决策

### 2.1 后台整体布局
- [x] 使用左侧菜单 + 顶部 header + 内容区布局
- [x] 首页先做轻量工作台，不做复杂大盘
- [x] 使用公共 `layouts` 结构
- [x] 面包屑一期不做

最终决策：
- 顶部为 `header`
- 中间区域为“左侧主侧边栏 + 右侧内容容器”
- 首页先做轻量工作台
- 面包屑一期不做

决策日期：2026-04-27

### 2.2 路由结构
- [x] 保留 `/login`
- [x] 保留 `/profile`
- [x] 保留 `/users`
- [x] 保留 `/users/:id`
- [x] 保留 `/users/:id/sessions`
- [x] 保留 `/users/:id/login-history`
- [x] 保留 `/audit-logs`
- [x] 保留 `403` / `404` 页面

最终决策：
- 保留以上全部路由
- 必须提供 `403` / `404` 页面

决策日期：2026-04-27

## 3. 权限与菜单决策

### 3.1 角色范围
- [x] 角色分为 `superAdmin / admin / user`
- [x] `superAdmin` 可见全部后台菜单
- [x] `admin` 不可访问审计日志
- [x] `admin` 不可管理 `superAdmin`
- [x] `user` 不允许进入后台管理页

最终决策：
- `superAdmin`：全部后台权限
- `admin`：用户管理相关权限，无审计日志权限，且不能管理 `superAdmin`
- `user`：只可使用个人相关能力，不进入后台管理页

决策日期：2026-04-27

### 3.2 菜单展示范围
- [x] `superAdmin` 显示全部菜单
- [x] `admin` 隐藏审计日志菜单
- [x] `user` 不显示后台菜单

最终决策：
- 菜单按角色动态显示

决策日期：2026-04-27

## 4. 页面展示决策

### 4.1 用户列表字段
- [x] 展示 `username`
- [x] 展示 `displayName`
- [x] 展示 `phone`
- [x] 展示 `role`
- [x] 展示 `status`

最终决策：
- 用户列表展示 `username / displayName / phone / role / status`

决策日期：2026-04-27

### 4.2 用户详情字段
- [x] 展示 `id`
- [x] 展示 `username`
- [x] 展示 `displayName`
- [x] 展示 `phone`
- [x] 展示 `role`
- [x] 展示 `status`
- [x] 展示 `lastLoginAt`
- [x] 展示当前是否在线

最终决策：
- 用户详情展示以上全部字段

决策日期：2026-04-27

### 4.3 会话字段
- [x] 展示 `sessionId`
- [x] 展示 `deviceType`
- [x] 展示 `deviceName`
- [x] 展示 `clientIp`
- [x] 展示 `userAgent`
- [x] 展示 `lastActiveAt`
- [x] 展示 `expiresAt`
- [x] 展示 `isRevoked`

最终决策：
- 会话页展示以上全部字段

决策日期：2026-04-27

### 4.4 登录历史与审计日志
- [x] `metadata` 直接按 JSON 字符串展示
- [x] `action` 做中文映射
- [x] `action` 颜色区分显示
- [x] 公共动作映射常量放在 `src/contants/index.ts`

最终决策：
- 登录历史与审计日志统一按以上策略展示

决策日期：2026-04-27

## 5. 页面操作决策

### 5.1 用户状态切换
- [x] 使用按钮 + `Popconfirm`
- [x] 禁用确认文案显示 `username`，没有则显示 `phone`
- [x] 不使用 `Switch`

最终决策：
- 禁用确认文案：`确认禁用 <username/phone> 用户吗？`
- 启用使用按钮 + `Popconfirm`

决策日期：2026-04-27

### 5.2 强制下线
- [x] 用户详情页提供入口
- [x] 用户会话页提供入口
- [x] 强制下线成功后刷新相关页面数据

最终决策：
- 详情页和会话页都提供强制下线入口
- 强制下线成功后刷新详情与会话数据

决策日期：2026-04-27

### 5.3 解封账号
- [x] 使用 `Modal + Form`
- [x] `reason` 非必填
- [x] 入口放在用户管理页右上角

最终决策：
- 解封账号入口固定放用户管理页右上角

决策日期：2026-04-27

### 5.4 刷新按钮
- [x] 用户列表页面标题旁提供刷新按钮
- [x] 用户详情页面标题旁提供刷新按钮
- [x] 用户会话页面标题旁提供刷新按钮
- [x] 用户登录历史页面标题旁提供刷新按钮
- [x] 审计日志页面标题旁提供刷新按钮
- [x] 刷新时保留分页
- [x] 刷新时 `PageCard` 显示 loading

最终决策：
- 无条件查询页面统一在 `card-head-title` 旁提供刷新按钮
- 刷新时复用页面已有 loading 状态

决策日期：2026-04-27

### 5.5 返回按钮
- [x] 用户详情页提供“返回列表”按钮
- [x] 用户会话页提供“返回详情”按钮
- [x] 用户登录历史页提供“返回详情”按钮

最终决策：
- 子页面必须提供方便回退的按钮，不依赖浏览器后退

决策日期：2026-04-27

## 6. Swagger 与返回格式对齐

### 6.1 成功响应
- [x] 统一解包 `{ code, msg, data }`
- [x] 页面只消费 `data`

最终决策：
- 前端统一解包 `{ code, msg, data }`

决策日期：2026-04-27

### 6.2 错误响应
- [x] 所有错误统一显示 `msg`
- [x] 对 `401 / 403` 做少量特殊处理
- [x] `code` 为数字类型
- [x] 接口字段为前端预期小驼峰

最终决策：
- 所有错误统一优先显示后端 `msg`
- `401 / 403` 做跳转或页面态处理
- `code` 必须是数字
- 返回字段统一为前端预期小驼峰

决策日期：2026-04-27

## 7. 样式与组件决策

### 7.1 样式分层
- [x] `unocss` 负责布局与间距
- [x] `scss` 负责复杂局部样式与 Ant Design 覆盖
- [x] 禁止大面积内联样式

最终决策：
- 统一按以上样式分层执行

决策日期：2026-04-27

### 7.2 公共组件
- [x] 实现 `RoleTag`
- [x] 实现 `StatusTag`
- [x] 实现统一卡片组件 `PageCard`

最终决策：
- 以上组件均为必需

决策日期：2026-04-27

## 8. 交付前必须拍板的最小清单
- [x] 登录页是否暴露注册入口
- [x] `user` 是否允许进入后台
- [x] `admin` 是否可见审计日志
- [x] 默认 `pageSize` 是否固定 `10`
- [x] 用户详情是否展示 `lastLoginAt`
- [x] 解封账号入口放在哪个页面
- [x] `metadata` 是否接受原始 JSON 展示
- [x] 是否需要 `403` / `404` 页面

## 9. 建议确认顺序
1. 登录页与登录态策略
2. 路由结构与后台布局
3. 角色菜单与访问权限
4. 各页面字段展示范围
5. 启用 / 禁用 / 强制下线 / 解封交互
6. Swagger 对齐与错误处理方式
7. 样式约束与公共组件约束

## 10. 回归复测最小清单
- [x] 登录成功
- [x] 登录失败（密码错误）
- [x] 冻结 / 禁用账号提示展示
- [x] refresh 自动续期
- [x] refresh 失败后跳转登录页
- [x] 当前用户信息展示
- [x] 当前会话展示
- [x] 用户列表分页
- [x] 用户详情展示
- [x] 用户禁用 / 启用
- [x] 用户会话查看
- [x] 用户登录历史查看
- [x] 审计日志查看
- [x] 解封账号操作
- [x] `admin` 无法查看 `superAdmin` 详情
- [x] `admin` 无法访问审计日志
- [x] `user` 不会误入后台管理页
- [x] 无条件查询页面刷新按钮（用户列表 / 用户详情 / 用户会话 / 用户登录历史 / 审计日志）
- [x] 用户详情 / 会话 / 登录历史页面返回按钮

## 11. 基于当前后端实现的复盘

### 11.1 已有后端能力
- [x] 已有认证接口：`/auth/login`、`/auth/refresh`、`/auth/logout`、`/auth/me`
- [x] 已有当前用户会话接口：`/sessions`、`/sessions/:sessionId`、`/sessions/logout-all`
- [x] 已有管理员用户管理接口
- [x] 已有用户会话、登录历史、审计日志查询接口
- [x] 已有冻结账号手动解封接口
- [x] 已有统一 Swagger 文档可联调

### 11.2 对前端最重要的限制
- [x] 单登录策略，不允许多端同时在线
- [x] 角色分为 `user / admin / superAdmin`
- [x] `admin` 权限小于 `superAdmin`
- [x] 登录失败按 `account` 维度冻结
- [x] 统一返回格式为 `{ code, msg, data }`

### 11.3 对前端开发顺序的建议
- 先搭请求层与路由守卫
- 再做登录主链路
- 然后做个人页与用户管理页
- 再做会话、登录历史、审计日志页
- 最后补解封操作、空状态、错误态与联调校验

## 12. 用户端登录 / 注册开关回归检查清单
- [x] 是否新增 `POST /api/v1/auth/login`
- [x] 是否新增 `POST /api/v1/auth/register`
- [x] 用户端登录是否允许 `user / admin / superAdmin`
- [x] 用户端注册是否只能创建 `user`
- [x] 注册开关默认值是否为关闭
- [ ] 开关关闭时，注册接口是否直接返回“当前未开放注册”或等价明确提示
- [ ] 开关开启后，用户端注册是否恢复可用
- [x] 后台工作台是否显示“开放用户端注册”配置项
- [x] `admin` 是否可见并可操作该开关
- [x] `superAdmin` 是否可见并可操作该开关
- [x] `user` 是否完全看不到该开关
- [x] 是否新增公共配置表 `system_settings`
- [x] 新库执行迁移后，是否自动包含默认配置 `public_user_register_enabled=false`
- [ ] 老库执行迁移后，是否自动补齐该表与默认配置

最终决策：
- 用户端登录单独提供接口，但仍允许 `user / admin / superAdmin` 登录
- 用户端注册单独提供接口，且只能注册 `user`
- 注册入口受系统配置 `public_user_register_enabled` 控制
- 默认值为关闭
- 配置入口放在后台工作台
- 仅 `admin / superAdmin` 可见并可操作
- 配置采用公共表 `system_settings` 持久化，便于后续扩展

决策日期：2026-04-27
